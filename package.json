{
  "name": "marketpulse-n8n-marketplace-trend-radar",
  "version": "1.1.0",
  "private": true,
  "description": "An n8n product opportunity radar for multiple marketplaces.",
  "scripts": {
    "validate": "python3 scripts/validate_workflow.py",
    "check:code": "node scripts/check-code-syntax.mjs",
    "demo": "node scripts/run-demo-path.mjs",
    "test": "python3 -m unittest discover -s tests -v && node scripts/check-code-syntax.mjs && node scripts/run-demo-path.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
